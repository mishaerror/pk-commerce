package com.pk.commerce.order.rest;

import org.springframework.web.bind.annotation.RestController;

@RestController
public class AdminOrderController {
    //filter all orders by merchant, date range, status

    //view order detail, with merchant contact info

    //complete order status on merchant request
}
